// Strict-input engine. Enforces wrong-char rejection and opposite-hand Shift.
// Populated in task 1C.

export function attach(inputEl, targetEl) {
  console.debug('stub: engine.attach', inputEl, targetEl);
}

export function detach() {
  console.debug('stub: engine.detach');
}
