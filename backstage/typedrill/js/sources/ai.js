// Source: IA (AI-generated text).
// Populated in Phase 3. Reuses backstage/js/ai-client.js, adds one new Worker action.
// Never clone ai-client.js.

export function generate(charset, stats, opts) {
  console.debug('stub: source.ai.generate', charset, stats, opts);
  return [];
}
