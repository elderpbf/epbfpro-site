// Source: Fraqueza (weakness-driven).
// Populated in Phase 2.

export function generate(charset, stats, opts) {
  console.debug('stub: source.weakness.generate', charset, stats, opts);
  return [];
}
