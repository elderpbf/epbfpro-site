// Source: Guiado (keybr-style unlock by speed threshold).
// Populated in Phase 4.

export function generate(charset, stats, opts) {
  console.debug('stub: source.guided.generate', charset, stats, opts);
  return [];
}
