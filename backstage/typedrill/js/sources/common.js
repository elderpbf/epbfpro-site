// Source: Palavras comuns pt-BR (top-1000).
// Populated in task 1J.

export function generate(charset, stats, opts) {
  console.debug('stub: source.common.generate', charset, stats, opts);
  return [];
}
