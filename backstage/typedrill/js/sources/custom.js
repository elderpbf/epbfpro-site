// Source: Texto personalizado (user-pasted).
// Populated in task 1K.

export function generate(charset, stats, opts) {
  console.debug('stub: source.custom.generate', charset, stats, opts);
  return [];
}
