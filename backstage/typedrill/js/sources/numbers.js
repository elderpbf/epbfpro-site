// Source: Números (digits only, optional Benford distribution).
// Populated in Phase 7.

export function generate(charset, stats, opts) {
  console.debug('stub: source.numbers.generate', charset, stats, opts);
  return [];
}
