// Paints the target area with ok/bad/cur/pending char classes.
// Populated in task 1F.

export function paint(targetEl, target, value) {
  console.debug('stub: renderer.paint', targetEl, target, value);
}
