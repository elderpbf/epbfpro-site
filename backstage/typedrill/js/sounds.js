// Optional error/key sounds. Off by default. Populated in Phase 9 refinements.

export function play(kind) {
  console.debug('stub: sounds.play', kind);
}

export function setMode(mode) {
  console.debug('stub: sounds.setMode', mode);
}
